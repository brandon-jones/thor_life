%p#notice= notice
%h1 Listing Game Instances
%table.table
  %thead
    %tr
      %th Game
      %th Server name
      %th Server address
      %th Server port
      %th Mod list
      %th
      - if current_user && current_user.super_admin?
        %th{:colspan => "4"}
  %tbody
    - @game_instances.each do |game_instance|
      %tr
        %td= link_to game_instance.game.name, games_path
        %td= game_instance.server_name
        %td= game_instance.server_address
        %td= game_instance.server_port
        %td= game_instance.mod_list
        %td= link_to 'Perks', game_instance
        - if current_user && current_user.super_admin?
          %td= link_to 'Show', game_instance
          %td= link_to 'Edit', edit_game_instance_path(game_instance)
          %td= link_to 'Destroy', game_instance, method: :delete, data: { confirm: 'Are you sure?' }
%br

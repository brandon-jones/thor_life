%p#notice= notice
%h1 Listing Groupings
%table
  %thead
    %tr
      %th Title
      %th{:colspan => "3"}
  %tbody
    - @groupings.each do |grouping|
      %tr
        %td= grouping.title
        %td= link_to 'Show', grouping
        %td= link_to 'Edit', edit_grouping_path(grouping)
        %td= link_to 'Destroy', grouping, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Grouping', new_grouping_path

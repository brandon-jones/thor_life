%p#notice= notice
%h1 Listing Cart Items
%table.table
  %thead
    %tr
      %th Item
      %th Price
      %th Cart
      %th{:colspan => "3"}
  %tbody
    - @cart_items.each do |cart_item|
      %tr
        %td
          = cart_item.item.class.to_s 
          #{link_to cart_item.item.title, "#{cart_item.item.class.to_s.downcase}s/#{cart_item.item.id}"}
        %td$#{cart_item.price}
        %td= link_to 'Cart', cart_path(cart_item.cart.id)
        %td= link_to 'Show', cart_item
        %td= link_to 'Edit', edit_cart_item_path(cart_item)
        %td= link_to 'Destroy', cart_item, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Cart item', new_cart_item_path
